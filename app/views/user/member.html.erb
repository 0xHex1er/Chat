<%= javascript_include_tag "jquery.js" %>
<%= javascript_include_tag "bootstrap.js" %>
<%= stylesheet_link_tag "bootstrap.css" %>
<style>
  .layout-store{
    border: grey thin solid;
    border-radius: 5px;
    min-height: 50px;
    margin: 5px;
    text-align: center;
    line-height: 50px;
  }
</style>
<div class="container">
  <div class="row">
    <h2> Member : <%= params[:username] %></h2>
    <hr>
    <div class="row">
      <div class="container">
        <% if @store.count > 0 %>
          <% @store.each do |st| %>
            <div class="col-xs-4 layout-store">
              <a href="../../chat/<%= st.id %>/<%= @login_user.id %>"><%= st.name %></a>
            </div>
          <% end %>
        <% end %>
      </div>
    <hr>
    <b>Go to : >> </b><a href="<%= fullpage_index_path(:user => params[:username]) %>">Full Web Page</a>
    </div>
  </div>
</div>
